[pytest]
pythonpath = .
testpaths = tests
markers =
    unit: fast, isolated unit tests
    integration: tests that exercise the API/DB together
    safety: safety-guard tests (added in Phase 5)
filterwarnings =
    ignore::DeprecationWarning
